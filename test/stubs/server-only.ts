// Stub for the `server-only` package under Vitest. In the app it throws if a
// server module is imported into a client bundle; in unit tests we import those
// modules directly on the server, so it's a harmless no-op.
export {};
